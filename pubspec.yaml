name: my_live_player
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_inappwebview: ^6.0.0
  permission_handler: ^11.3.0
  # 【绝对关键】降级到 5.1.0，这是云端打包最不容易报错的版本
  ffmpeg_kit_flutter_https_gpl: 5.1.0
  path_provider: ^2.1.2
  gal: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
